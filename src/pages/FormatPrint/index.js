import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const FormatPrint = () => {
  return (
    <View>
      <Text> Format Print</Text>
    </View>
  );
};

export default FormatPrint;

const styles = StyleSheet.create({});
